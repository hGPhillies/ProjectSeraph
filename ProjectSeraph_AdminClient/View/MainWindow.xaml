<Window x:Class="ProjectSeraph_AdminClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ProjectSeraph_AdminClient.View"
        xmlns:viewmodel="clr-namespace:ProjectSeraph_AdminClient.ViewModel"
        Title="MainWindow" Height="650" Width="900">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodel:CitizenViewModel}">
            <views:CitizenView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:MeasurementsViewModel}">
            <views:MeasurementsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:AlarmsViewModel}">
            <views:AlarmsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:ContactViewModel}">
            <views:ContactView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:StatisticsViewModel}">
            <views:StatisticsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:ManageNursesViewModel}">
            <views:ManageNursesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:ManageCitizenViewModel}">
            <views:ManageCitizenView />
        </DataTemplate>

        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width" To="110" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="Height" To="25" Duration="0:0:0.3"/>
                                <ColorAnimation Storyboard.TargetProperty="Background.(SolidColorBrush.Color)" To="LightGray" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>

                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width" To="100" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="Height" To="20" Duration="0:0:0.3"/>
                                <ColorAnimation Storyboard.TargetProperty="Background.(SolidColorBrush.Color)" To="LightSlateGray" Duration="0:0:0.3"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="150"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="11,10,11,10">
                <Image Source="pack://application:,,,/ProjectSeraph_AdminClient;component/Resources/seraph_logo.png"
                       Height="50" Margin="0,0,0,20"/>
                <Button Height="20" Width="100" Background="LightSlateGray" Content="Borger" Command="{Binding NavigationCommand}" Style="{StaticResource NavigationButtonStyle}" CommandParameter="Citizen" Margin="0,5"/>
                <Button Height="20" Width="100" Background="LightSlateGray" Content="Målinger" Command="{Binding NavigationCommand}" Style="{StaticResource NavigationButtonStyle}" CommandParameter="Measurements" Margin="0,5"/>
                <Button Height="20" Width="100" Background="LightSlateGray" Content="Alarmer" Command="{Binding NavigationCommand}" Style="{StaticResource NavigationButtonStyle}" CommandParameter="Alarms" Margin="0,5"/>
                <Button Height="20" Width="100" Background="LightSlateGray" Content="Kontakt" Command="{Binding NavigationCommand}" Style="{StaticResource NavigationButtonStyle}" CommandParameter="Contact" Margin="0,5"/>
                <Button Height="20" Width="100" Background="LightSlateGray" Content="Statistik" Command="{Binding NavigationCommand}" Style="{StaticResource NavigationButtonStyle}" CommandParameter="Statistics" Margin="0,5"/>
                <Button Height="20" Width="100" Background="LightSlateGray" Content="Redigér profiler" Command="{Binding NavigationCommand}" Style="{StaticResource NavigationButtonStyle}" CommandParameter="Editor" Margin="0,5"/>
                <Button Height="20" Width="100" Background="LightSlateGray" Content="Redigér Borger" Command="{Binding NavigationCommand}" Style="{StaticResource NavigationButtonStyle}"  CommandParameter="Manage Citizen" Margin="0,5"/>
            </StackPanel>

            <Border Grid.Column="1" Margin="10,10,10,10" BorderBrush="Gray" BorderThickness="1">
                <ContentControl Content="{Binding CurrentViewModel}"/>
            </Border>
        </Grid>

        <Border Grid.Row="2" Height="20" Background="LightGray"/>
    </Grid>
</Window>