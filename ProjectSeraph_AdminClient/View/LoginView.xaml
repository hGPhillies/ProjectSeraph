<UserControl x:Class="ProjectSeraph_AdminClient.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">

    <Grid Background="White">
        <Border Margin="50" Padding="30" CornerRadius="10" BorderBrush="LightSlateGray" BorderThickness="1">
            <StackPanel VerticalAlignment="Center">
                
                <Image Source="pack://application:,,,/ProjectSeraph_AdminClient;component/Resources/seraph_logo.png"
                       Height="80" Margin="0,0,0,30"/>
                
                <TextBlock Text="Admin Login" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20" 
                           Foreground="#333"/>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Brugernavn (Username)" Margin="0,0,0,5" Foreground="#555"/>
                    <TextBox Text="{Binding Username}" 
                             Padding="5" 
                             Height="30" 
                             BorderBrush="LightGray"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Adgangskode (Password)" Margin="0,0,0,5" Foreground="#555"/>
                    <PasswordBox x:Name="PasswordBox" 
                                 Padding="5" 
                                 Height="30" 
                                 BorderBrush="LightGray"/>
                </StackPanel>

                <Button Content="Log Ind" 
                        Command="{Binding LoginCommand}" 
                        Background="LightSlateGray" 
                        Foreground="White" 
                        FontSize="16" 
                        Height="40" 
                        Margin="0,10,0,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="IsDefault" Value="True"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5" 
                                                Padding="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="LightGray"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
