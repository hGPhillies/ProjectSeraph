<UserControl x:Class="ProjectSeraph_AdminClient.View.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top-bar: vælg dato + se antal målinger + refresh -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Vælg dato:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker SelectedDate="{Binding SelectedDate}" Margin="0,0,10,0"/>

            <TextBlock Text="{Binding SelectedDateCount, StringFormat='Målinger denne dag: {0}'}"
                       VerticalAlignment="Center" Margin="0,0,20,0"/>

            <Button Content="Opdater"
                    Command="{Binding RefreshCommand}"
                    Padding="10,2"
                    VerticalAlignment="Center"/>
        </StackPanel>

        <!-- LiveCharts søjlediagram -->
        <lvc:CartesianChart Grid.Row="1"
                            Series="{Binding SeriesCollection}">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Labels="{Binding Labels}" Title="Dato"/>
            </lvc:CartesianChart.AxisX>
            <!-- Y-akse konfiguration. Step="1" = der laves kun streger for 0,1,2 osv.-->
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Antal målinger"
              MinValue="0"
              MaxValue="10">
                    <lvc:Axis.Separator>
                        <lvc:Separator Step="1" />
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>

    </Grid>
</UserControl>
